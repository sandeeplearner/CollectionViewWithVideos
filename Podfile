# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CollectionViewTesting' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod 'Kingfisher', '~> 4.0'
    pod 'RxAVFoundation'
  # Pods for CollectionViewTesting

  target 'CollectionViewTestingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CollectionViewTestingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
